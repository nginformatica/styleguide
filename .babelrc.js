module.exports = {
    plugins: [
        '@babel/plugin-transform-runtime',
        'transform-class-properties'
    ],
    presets: [
        '@babel/preset-env',
        '@babel/preset-react',
        '@babel/preset-typescript'
    ]
}
